A reactivation request was submitted for the user [url=/user.php?id={{ user.ID }}]{{ user.Username }}[/url] with the email {{ email.Address }} which belongs to the user [url=/user.php?id={{ email.user.ID }}]{{ email.user.Username }}[/url].

The inactivity estimate given was {{ info }}.

Request IP: [url=/user.php?action=search&action=search&ip={{ ip }}]{{ ip }}[/url]
{# TODO Leaving IP for now as it was requested but fix needed for {{ render.geoip(ip) }} #}