A password reset was completed for [url=/user.php?id={{ user.ID }}]{{ user.Username }}[/url].


The user's last known IP was {{ ip }}, with origin {{ ipcc }}.

The requesting IP was {{ rip }}, with origin {{ ripcc }}.
