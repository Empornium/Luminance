{% if options.useSpan == false %}
    {{ group.Name|striptags|raw }}
{% else %}
    <span alt="{{ group.ID }}" class="groupperm" title="{{ group.Description|striptags|raw }}" style="color:#{{ group.Color }}" title="{{ group.Description|striptags|raw }}">{{ group.Name|striptags|raw }}</span>
{% endif %}
